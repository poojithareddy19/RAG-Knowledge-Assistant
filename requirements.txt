# Core
# 1.52 is the floor because the UI passes width="stretch" rather than the
# deprecated use_container_width, and the width API landed across 1.50 to 1.52.
streamlit>=1.52,<2.0
# <4.0, not <3.0: the suite runs green on pandas 3, which is what a fresh
# install resolves to and what the development venv already had while this
# file still claimed otherwise.
pandas>=2.2,<4.0
numpy>=1.26,<3.0
pyyaml>=6.0
python-dotenv>=1.0

# ARGO NetCDF ingestion
xarray>=2024.3
netCDF4>=1.6

# Embeddings for the semantic index
sentence-transformers>=3.0,<4.0

# LLM provider
requests>=2.31

# Tracing: spans to logs/traces.jsonl, or any OTLP collector when configured
opentelemetry-sdk>=1.30,<2.0
opentelemetry-exporter-otlp-proto-http>=1.30,<2.0

# Testing
pytest>=8.0

# Database, with pgvector for the summaries
psycopg[binary,pool]>=3.2
pgvector>=0.3
sqlparse>=0.5
scipy>=1.13
matplotlib>=3.8
plotly>=5.22
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

ruff~=0.16.0
