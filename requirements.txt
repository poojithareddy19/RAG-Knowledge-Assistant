# Core
# 1.52 is the floor because the UI passes width="stretch" rather than the
# deprecated use_container_width, and the width API landed across 1.50 to 1.52.
streamlit>=1.52,<2.0
pandas>=2.0,<3.0
numpy>=1.24,<3.0
pyyaml>=6.0
python-dotenv>=1.0

# Ingestion
pypdf>=4.0,<6.0
python-docx>=1.1

# ARGO NetCDF ingestion
xarray>=2024.3
netCDF4>=1.6

# Chunking (only framework dependency; isolated to src/ingestion/chunker.py)
langchain-text-splitters>=0.2,<0.4

# Embeddings + reranking
sentence-transformers>=3.0,<4.0

# Vector store
faiss-cpu>=1.8

# LLM provider
requests>=2.31

# Testing
pytest>=8.0

psycopg[binary,pool]>=3.2
pgvector>=0.3
sqlparse>=0.5
pandas>=2.2
numpy>=1.26
scipy>=1.13
matplotlib>=3.8
plotly>=5.22
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

# Model Context Protocol server (src/mcp/server.py). 2.x, not the 1.2 the
# plan named: the handler API this server is written against is 2.x, and a 1.x
# install would import and then fail to start.
mcp>=2.2
pytest>=8.0
ruff>=0.5